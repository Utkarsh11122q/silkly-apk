{
  "name": "silkly-apk",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that builds the Silkly Android APK",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "sharp": "^0.35.2"
  }
}
